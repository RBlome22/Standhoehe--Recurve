## brace-height-en/index.html
76: <div id="statusBox" class="status">Select photo. Danach linke und rechte Kante der Scheibenauflage antippen. Anschließend die arrows antippen.</div>
102: <section class="card printOnly"><h2>Versuchsablauf</h2><p>Je Standhöhe werden 6 arrows geschossen. Pro end wird ein Outlier gestrichen. Die App berechnet aus den verbleibenden impactn die konvexe groupsfläche in cm². Die Standhöhe mit dem kleinsten Areaninhalt wird als optimum Standhöhe markiert.</p></section>
124: function drawPhoto(){const c=$("photoCanvas"),ctx=c.getContext("2d");ctx.setTransform(1,0,0,1,0,0);ctx.clearRect(0,0,c.width,c.height);ctx.fillStyle="#111";ctx.fillRect(0,0,c.width,c.height);if(state.image){let img=state.image,iw=img.naturalWidth||img.width,ih=img.naturalHeight||img.height,r=Math.min(c.width/iw,c.height/ih),w=iw*r,h=ih*r,x=(c.width-w)/2,y=(c.height-h)/2;try{ctx.drawImage(img,x,y,w,h)}catch(e){ctx.fillStyle="#fff";ctx.fillText("The photo cannot be displayed",c.width/2,c.height/2)}}else{ctx.fillStyle="#ddd";ctx.font="22px system-ui";ctx.textAlign="center";ctx.fillText("Select photo oder mit Camera aufnehmen",c.width/2,c.height/2)}
127: if(res&&res.hull.length>=3){ctx.beginPath();ctx.moveTo(res.hull[0].x,res.hull[0].y);for(let i=1;i<res.hull.length;i++)ctx.lineTo(res.hull[i].x,res.hull[i].y);ctx.closePath();ctx.fillStyle="rgba(54,214,107,.18)";ctx.fill();ctx.strokeStyle="#36d66b";ctx.lineWidth=3;ctx.stroke();setStatus(`Current end: ${state.arrows.length} arrows, Outlier arrow ${drop+1}, Area ${res.areaCm2.toFixed(2)} cm², group diameter ${res.groupDiameterMm.toFixed(1)} mm.`)}else if(state.image){if(state.calib.length<2)setStatus("Photo loaded. Please mark two calibration points.");else setStatus(`Kalibrierung vorhanden. Please Mark arrow impacts. Aktuell: ${state.arrows.length}.`)}}
131: $("modeCalib").onclick=()=>{state.mode="calib1";state.calib=[];drawPhoto();setStatus("Reset calibration: zwei Kanten der Scheibenauflage antippen.")};
133: $("saveendsBtn").onclick=()=>{let res=computeCurrent(),need=Number($("arrowsPerEnd").value||6),bh=Number($("braceHeight").value);if(!res||state.arrows.length<need){setStatus(`Please Kalibrierung setzen und ${need} Mark arrow impacts.`,true);return}if(!bh){setStatus("Please enter the brace height.",true);return}let entry={id:Date.now(),date:new Date().toLocaleString("en-GB"),seriesNo:Number($("seriesNo").value||state.series.length+1),braceHeight:bh,braceUnit:$("braceUnit").value,areaCm2:res.areaCm2,groupDiameterMm:res.groupDiameterMm,droppedArrow:res.dropIndex+1,targetDiameterCm:targetDiameterCm(),distance:Number($("distance").value||18),distanceUnit:$("distanceUnit").value};let idx=state.series.findIndex(s=>s.seriesNo===entry.seriesNo);if(idx>=0)state.series[idx]=entry;else state.series.push(entry);state.series.sort((a,b)=>a.seriesNo-b.seriesNo);localStorage.setItem("brace_series_wa_v1",JSON.stringify(state.series));$("seriesNo").value=Number($("seriesNo").value||1)+1;state.arrows=[];state.calib=[];state.mode="calib1";drawAll();setStatus("end gespeichert. Nächste Standhöhe schießen und neues Select photo.")};
136: function renderTable(){let b=best();if(!state.series.length){$("tableWrap").innerHTML="<p class='hint'>No ends saved yet.</p>";return}let h="<table><thead><tr><th>end</th><th>Standhöhe</th><th>Area</th><th>groups-Ø</th><th>gestrichen</th><th>Date</th><th class='noPrint'>Action</th></tr></thead><tbody>";for(const s of state.series){let ib=b&&s.id===b.id;h+=`<tr class="${ib?'best':''}"><td>${s.seriesNo} ${ib?'<span class="pill">optimum</span>':''}</td><td>${s.braceHeight.toFixed(1)} ${s.braceUnit}</td><td><b>${s.areaCm2.toFixed(2)} cm²</b></td><td>${s.groupDiameterMm.toFixed(1)} mm</td><td>arrow ${s.droppedArrow}</td><td>${s.date}</td><td class="noPrint"><button class="secondary" onclick="deleteends(${s.id})">delete</button></td></tr>`}h+="</tbody></table>";$("tableWrap").innerHTML=h}
138: function renderBest(){let b=best();if(!b){$("bestResult").className="status warn";$("bestResult").textContent="No ends saved yet.";return}$("bestResult").className="status";$("bestResult").innerHTML=`Optimum brace height: <b>${b.braceHeight.toFixed(1)} ${b.braceUnit}</b> with <b>${b.areaCm2.toFixed(2)} cm²</b>. Arrow discarded: ${b.droppedArrow}.`}

## bare-shaft-tuner-en/index.html
41: <div class="actions"><button class="secondary" id="backSetup">Back</button><button class="primary" id="goFletched">Starting position hergestellt – Messserie beginnen</button></div>
52: <p><b>3 bare shafts at 16 m</b> schießen. The fletched arrows may be removed first. This photo is corrected separately using the target-face corners.</p>
62: <div class="actions"><button class="secondary" id="undo">Letzten Punkt löschen</button><button class="danger" id="resetMarks">Clear all marks</button><button class="primary" id="savePhoto" disabled>Accept 3 impacts</button></div>
86: var state={testMode:'new',hand:'right',bowType:'olympic',targetSize:40,arrowLength:null,arrowSpine:null,maxTurns:0,top:1,bottom:0,cumAngle:0,lastDirection:0,nockStep:0,groups:{fletched:null,blank:null},photoKind:null,tillerHistory:[],pendingAdjustment:null,optimal:null,verification:false,last:null};
90: function targetText(){var side=state.hand==='right'?'left':'right';return '<b>Grundziel '+(state.hand==='right'?'Right-handed':'Left-handed')+':</b> bare shafts leicht <b>'+side+'</b> und leicht <b>lower</b> als die fletched group. Die konkrete zulässige target zone wird aus der measured grouping des archer angepasst.'}
93: function updateSetupInfo(){var correction=q('testMode').value==='correction';q('maxTurnsWrap').classList.toggle('hidden',correction);q('modeInfo').innerHTML=correction?'<b>Correction:</b> The current limb-bolt position is treated internalally as 0°. Additional turning out is limited to <b>+360° (= 1 turn)</b>. Turning in may continue as long as the manufacturer’s mechanical limit is respected.':'<b>New setup:</b> Starting position wird definiert hergestellt. Die absolute manufacturer limit für das Herausdrehen muss angegeben werden.';var hand=q('hand').value;q('targetInfo').innerHTML='<b>Target zone:</b> '+(hand==='right'?'Right-handed → bare shafts leicht left und leicht lower.':'Left-handed → bare shafts leicht right und leicht lower.')}
95: q('goStart').onclick=function(){var correction=q('testMode').value==='correction',m=parseFloat(q('maxTurns').value),e=q('setupError');e.classList.add('hidden');if(!correction&&(!isFinite(m)||m<=0)){e.textContent='Please enter the maximum number of turns out permitted by the manufacturer.';e.classList.remove('hidden');return}state.testMode=correction?'correction':'new';state.hand=q('hand').value;state.bowType=q('bowType').value;state.targetSize=parseInt(q('targetSize').value,10)||40;state.arrowLength=parseFloat(q('arrowLength').value)||null;state.arrowSpine=parseInt(q('arrowSpine').value,10)||null;state.maxTurns=correction?0:m;state.cumAngle=0;state.lastDirection=0;state.nockStep=0;state.tillerHistory=[];state.verification=false;state.optimal=null;if(correction){q('startText').innerHTML='<div class="notice"><b>Correction:</b> Do not change the current setup. It is the 0° starting point. Turn out by no more than +1 turn; turn in as required up to the mechanically permitted limit.</div>'}else if(state.bowType==='olympic'){state.top=1;state.bottom=0;q('startText').innerHTML='<div class="notice"><b>Olympic recurve:</b><ol><li>Beide limb bolts carefully turn fully in.</li><li>Upper limb bolt <b>1 turn out</b>.</li><li>Lower limb bolt bleibt fully in.</li></ol>Weitere draw weightskorrekturen erfolgen anschließend an beiden limb bolts equally.</div>'}else{state.top=0;state.bottom=0;q('startText').innerHTML='<div class="notice"><b>Barebow:</b> Beide limb bolts carefully turn fully in. Diese equallye Stellung ist der Ausgang.</div>'}show('start')};
105: function renderMarks(){var next=pts.length<7?markNames[pts.length]:'All marks set';q('prompt').innerHTML=pts.length<7?'<b>'+(pts.length+1)+'/7:</b> '+next+' markieren.':'<b>All marks set.</b>';var h='';markNames.forEach(function(n,i){h+='<div class="'+(i===pts.length?'now':'')+'">'+(i<pts.length?'✓ ':'')+n+'</div>'});q('legend').innerHTML=h;q('savePhoto').disabled=pts.length!==7}
106: function autoTargetGuess(){if(!image)return false;var iw=image.naturalWidth||image.width,ih=image.naturalHeight||image.height,m=.07,guess=[{x:iw*m,y:ih*m},{x:iw*(1-m),y:ih*m},{x:iw*(1-m),y:ih*(1-m)},{x:iw*m,y:ih*(1-m)}];pts=guess;q('autoStatus').textContent='Automatischer Vorschlag gesetzt. Gelben Rahmen prüfen. Wenn er nicht exakt an der Auflage liegt, „Reset corners manually“ wählen.';draw();return true}
122: else{if(!state.verification){state.optimal={angle:state.cumAngle,dx:m.dx,dy:m.dy,stiffward:m.stiffward,target:t};var dir=state.lastDirection||1,deg=Math.min(90,Math.max(15,state.pendingAdjustment&&state.pendingAdjustment.deg||30));if(t.avg>50)deg=Math.max(20,deg);if(state.testMode==='correction'&&dir>0)deg=Math.min(deg,360-state.cumAngle);if(deg>=5){state.verification=true;state.pendingAdjustment={type:'verify',dir:dir,deg:Math.round(deg)};rec+='<div class="notice"><b>Archer-adapted optimum zone reached.</b><br>This setting is adapted to this archer’s measured grouping. For confirmation, make only one small verification step: both limb bolts <b>'+Math.round(deg)+'° '+(dir>0?'out':'in')+'</b>. Then shoot both groups again.</div>';q('applyTiller').classList.remove('hidden')}else{rec+='<div class="notice"><b>Archer-adapted optimum zone reached.</b><br>This setting is adapted to this archer’s measured grouping.</div>';q('finishOptimal').classList.remove('hidden')}}else{var back=state.cumAngle-state.optimal.angle;if(Math.abs(back)>=1){rec+='<div class="notice"><b>Optimum confirmed.</b><br>Now reverse only the last adjustment: both limb bolts <b>'+Math.abs(Math.round(back))+'° '+(back>0?'turn in':'turn out')+'</b>. The optimum setting determined for this archer is then restored.</div>';state.pendingAdjustment={type:'return',dir:back>0?-1:1,deg:Math.abs(Math.round(back))};q('finishOptimal').classList.remove('hidden')}else{q('finishOptimal').classList.remove('hidden')}}}
126: q('finishOptimal').onclick=function(){var text;if(state.verification&&state.optimal){var back=state.cumAngle-state.optimal.angle;if(Math.abs(back)>=1){text='<div class="notice"><b>Before finishing:</b> Beide limb bolts noch <b>'+Math.abs(Math.round(back))+'° '+(back>0?'turn in':'turn out')+'</b>, to return exactly to the stored optimum position.</div>'}else text='<div class="notice"><b>The optimum setting has been restored.</b></div>';text+='<div class="notice blue"><b>Archer-adapted tuning.</b> This setting was determined from this archer’s measured grouping and repeatability.</div><div class="metrics"><div class="metric">Target horizontal<b>'+fmt(state.optimal.dx,1)+' mm</b></div><div class="metric">Ziel vertical<b>'+fmt(state.optimal.dy,1)+' mm</b></div><div class="metric">Nocking-point correction<b>'+(state.nockStep>=0?'+':'')+state.nockStep+' steps</b></div></div>'}else{text='<div class="notice"><b>Test ended.</b> Use the best setting currently achievable.</div>'}q('doneText').innerHTML=text;show('done')};

## bow-setup-assistant-en/index.html
23: <div class="actions"><button class="primary" id="startWizard">Start combined setup</button><a class="btn secondary" href="https://rblome22.github.io/Standhoehe--Recurve/brace-height-en/" target="_blank">Nur Brace height öffnen</a><a class="btn secondary" href="https://rblome22.github.io/Standhoehe--Recurve/bare-shaft-tuner-en/" target="_blank">Nur Bare shaft öffnen</a></div>
27: <div class="notice">Führe die Brace heightnmessung vollständig durch. Danach bottom auf <b>„Brace height abgeschlossen“</b> drücken. Wenn die App im gleichen Browser läuft, versucht der Assistent das beste Ergebnis automatisch zu übernehmen.</div>
29: <div class="actions"><button class="primary" id="braceDone">Brace height abgeschlossen – Ergebnis übernehmen</button><button class="secondary" id="backIntro">Back to overview</button></div>
35: <div class="notice warn"><b>Note:</b> Der groupsdurchmesser dient hier nur als Orientierung für den Leistungsstand. Der Bare shaft-Tuner bestimmt seine personal Toleranz anschließend neu aus der aktuellen fletchedn Referenzgruppe.</div>
36: <div class="actions"><button class="primary" id="goBlank">Weiter zur Bare shaft-Abstimmung</button><button class="secondary" id="backBrace">Back zur Brace height</button></div>
39: <h2>Phase 2 – Bare shaft, nocking point und Tiller</h2>
47: <div class="notice blue"><b>Archer-adapted setup:</b> Die Bare shaft-Abstimmung berücksichtigt die aktuelle grouping des archer. Bei später deutlich verbesserter Schießtechnik empfiehlt sich eine againe Korrektur.</div>
54: var ids=['intro','brace','transfer','blank','done'],names=['Übersicht','Brace height','Übernahme','Bare shaft','Done'];
59: q('braceDone').onclick=function(){var b=readBrace();if(b){q('braceHeight').value=(isFinite(b.height)?b.height.toLocaleString('en-GB',{maximumFractionDigits:1}):'')+' '+b.unit;q('groupDiameter').value=isFinite(b.diam)?b.diam.toLocaleString('en-GB',{maximumFractionDigits:1})+' mm':'';q('transferMsg').innerHTML='<b>Brace heightn-Ergebnis gefunden.</b><br>Best end: '+b.series+' · Brace height '+b.height.toLocaleString('en-GB',{maximumFractionDigits:1})+' '+b.unit+' · Group area '+b.area.toLocaleString('en-GB',{maximumFractionDigits:2})+' cm² · groups-Ø '+b.diam.toLocaleString('en-GB',{maximumFractionDigits:1})+' mm.'}else{q('transferMsg').innerHTML='<b>No saved result was found automatically.</b><br>This can happen with a locally opened file or a different browser. The two values can be entered manually below.'}show('transfer')};
60: q('goBlank').onclick=function(){var bh=q('braceHeight').value.trim(),gd=q('groupDiameter').value.trim();q('carryInfo').innerHTML='<b>Phase 1 completed.</b>'+(bh?'<br>Übernommene optimum Brace height: <b>'+bh+'</b>.':'')+(gd?'<br>Reference grouping from phase 1: <b>'+gd+'</b>.':'')+'<br>Der Bare shaft-Tuner ermittelt seine Toleranz jetzt again aus den 3 fletchedn Referenzpfeilen.';show('blank')};
